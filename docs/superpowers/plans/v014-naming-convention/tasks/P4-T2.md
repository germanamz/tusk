# [v0.14 P4-T2] Drop internal/tui/ pathfilter entry

> Type: task
> Parent: P4 — Sweep internal/tui/
> Plan: docs/superpowers/plans/v014-naming-convention/04-sweep-tui.md (task 2)

## What

Delete the regex line for
`^github\.com/germanamz/tusk/internal/tui(/|$)` from the
`excluded` slice in `internal/lint/pathfilter/pathfilter.go`.

## Why

Brings rules 2, 3, 4 into scope for `internal/tui/`.

## Code references

- `internal/lint/pathfilter/pathfilter.go` — locate the entry matching `internal/tui`.

## Acceptance

- `internal/lint/pathfilter/pathfilter.go`'s `excluded` slice no longer contains the `internal/tui` entry.
- All other entries remain.

## Bridge code

Removes: `internal/tui/` regex entry in `pathfilter.go` (introduced by P2-T4).

## Depends on

P2 complete.

## Blocks

P4-T3.
