# [v0.14 P2-T5] Register analyzers in cmd/tusk-lint

> Type: task
> Parent: P2 — Custom analyzers (rules 2, 3, 4)
> Plan: docs/superpowers/plans/v014-naming-convention/02-custom-analyzers.md (task 5)
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §2

## What

Replace the empty analyzer list in `cmd/tusk-lint/main.go` (bridge
code from P1-T3) with `multichecker.Main(blankline.Analyzer,
namederr.Analyzer, testhandle.Analyzer)`. Remove the bridge-code
comment.

## Why

Activates all three rules in the linter binary. After this task
ships, `tusk-lint` reports the rule violations defined in P2-T1
through P2-T3 wherever the package is not excluded by the
pathfilter.

## Code references

- `cmd/tusk-lint/main.go` — currently calls `multichecker.Main()` with no analyzers (bridge code from P1-T3).
- `internal/lint/blankline`, `internal/lint/namederr`, `internal/lint/testhandle` — packages from P2-T1, P2-T2, P2-T3.

## Acceptance

- `cmd/tusk-lint/main.go` imports the three analyzer packages.
- `multichecker.Main(...)` is called with all three analyzers.
- The Phase-1 bridge-code comment is gone.
- `tusk-lint -blankline ./...`, `tusk-lint -namederr ./...`, and `tusk-lint -testhandle ./...` each run independently.
- `tusk-lint ./...` runs all three.
- `make lint` continues to exit zero against the unmodified codebase (everything is path-excluded).

## Bridge code

Removes: empty analyzer registry in `cmd/tusk-lint/main.go` (introduced by P1-T3).

## Depends on

P2-T1, P2-T2, P2-T3 — analyzer packages must exist.
P2-T4 — pathfilter must be in place so registration doesn't surface unexpected violations.

## Blocks

P2-T6.
