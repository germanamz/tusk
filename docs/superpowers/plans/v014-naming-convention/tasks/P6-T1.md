# [v0.14 P6-T1] Drop filter/ + domain/ + syntax/ varnamelen exclusions

> Type: task
> Parent: P6 — Sweep filter/ + domain/ + syntax/
> Plan: docs/superpowers/plans/v014-naming-convention/06-sweep-filter-domain.md (task 1)

## What

Delete the three `linters: [varnamelen]` rules whose `path` is
`^filter/`, `^domain/`, and `^syntax/` from `.golangci.yml`. Other
per-package rules stay untouched.

## Why

Brings rule 1 into scope for all three packages.

## Code references

- `.golangci.yml` `linters.exclusions.rules` — locate entries with `path: ^filter/`, `path: ^domain/`, `path: ^syntax/`.

## Acceptance

- None of the three rules exists in `.golangci.yml` after the change.
- All other rules remain.

## Bridge code

Removes: three `varnamelen` exclusion rules (introduced by P1-T5).

## Depends on

P2 complete.

## Blocks

P6-T3.
