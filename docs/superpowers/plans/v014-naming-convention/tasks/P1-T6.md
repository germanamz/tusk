# [v0.14 P1-T6] Verify Phase 1 CI green

> Type: task
> Parent: P1 — Convention doc + linter scaffold + rule 1
> Plan: docs/superpowers/plans/v014-naming-convention/01-convention-and-scaffold.md (task 6)

## What

Run the full local validation suite to confirm the Phase 1 changes
are CI-clean: `make build`, `make test`, `make lint`, and a
representative pre-commit hook execution.

## Why

Phase 1 introduces no behavior changes but does enable a new linter
and add a new lint target. This task closes the phase by proving
all three gates stay green.

## Code references

- `Makefile` — `build`, `test`, `lint` targets.
- `lefthook.yml` (or equivalent) — pre-commit hooks if any exist in the repo.

## Acceptance

- `make build` exits zero.
- `make test` exits zero.
- `make lint` exits zero (both `lint-go` and `lint-tusk` pass; tusk-lint reports no violations because no analyzers are registered).
- Pre-commit hooks pass when committing the phase's changes.

## Bridge code

None.

## Depends on

P1-T1, P1-T2, P1-T3, P1-T4, P1-T5 — all prior Phase 1 tasks.

## Blocks

P2 (entire phase 2 cannot start until P1 is verified green).
