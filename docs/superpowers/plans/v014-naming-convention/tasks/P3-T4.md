# [v0.14 P3-T4] Verify service/ tests pass

> Type: task
> Parent: P3 — Sweep service/
> Plan: docs/superpowers/plans/v014-naming-convention/03-sweep-service.md (task 4)

## What

Run `make test` to verify behavior is preserved after the sweep.
All existing service-layer tests must pass. Failures indicate an
accidental semantic change during the sweep — investigate and fix.

## Why

Mechanical renames can accidentally change semantics (e.g.,
shadowing a different identifier, mis-typed `:=` vs `=`). Test
suite is the regression net.

## Code references

- `service/*_test.go` — full test suite for the package.

## Acceptance

- `make test` exits zero.
- No accidental semantic changes — the diff still reads as mechanical.
- If failures surface, root-cause and fix as part of this task before P3-T5.

## Bridge code

None.

## Depends on

P3-T3.

## Blocks

P3-T5.
