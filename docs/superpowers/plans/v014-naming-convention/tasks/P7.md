# [v0.14 P7] Sweep repository/ + sqlite/ + cmd/ + tests/e2e/ + root

> Type: epic (phase parent)
> Milestone: v0.14
> Plan: docs/superpowers/plans/v014-naming-convention/07-sweep-rest.md
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §3

## What

Bring `repository/`, `sqlite/`, `cmd/`, `tests/e2e/`, and the root
package (`client.go`, `client_test.go`) into compliance with
STYLE.md's four rules. Remove the five package-level exclusion
entries from both linters. After this phase (combined with Phases
3–6), every package in the repository is clean.

## Why

This phase closes out the remaining production and test code.
`cmd/tusk-lint/` (introduced in Phase 1) is also swept here — it
must already be clean since it was written under the convention,
but the linter verifies it.

## Children

- P7-T1: Drop remaining varnamelen exclusions
- P7-T2: Drop remaining pathfilter entries
- P7-T3: Apply STYLE.md fixes across remaining packages
- P7-T4: Verify e2e and unit tests pass
- P7-T5: Verify P7 packages lint clean

## Acceptance

- `make lint` passes against all five packages with no exclusions.
- `make test` and `make test-race` pass — every repository implementation, every CLI scenario, every entry-point behavior unchanged.
- Default DB path resolution and migration behavior unchanged.

## Depends on

P2 complete.

## Parallelizable with

P3, P4, P5, P6.

## Blocks

P8.
