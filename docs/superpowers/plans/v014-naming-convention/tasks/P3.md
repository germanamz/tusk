# [v0.14 P3] Sweep service/

> Type: epic (phase parent)
> Milestone: v0.14
> Plan: docs/superpowers/plans/v014-naming-convention/03-sweep-service.md
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §3

## What

Bring every file in `service/` (production and tests) into
compliance with STYLE.md's four rules. Remove the `service/`
exclusion entries from both linters. Mechanical sweep; no behavior
changes.

## Why

`service/` is the highest-traffic package and the original
motivating example for the convention (see audit citations of
`service/task.go`). Sweeping it first proves the convention against
real call sites before parallel sweeps roll it out further.

## Children

- P3-T1: Drop service/ varnamelen exclusion
- P3-T2: Drop service/ pathfilter entry
- P3-T3: Apply STYLE.md fixes across service/
- P3-T4: Verify service/ tests pass
- P3-T5: Verify service/ lint clean

## Acceptance

- `make lint` passes against `service/` with no exclusions.
- `make test` passes — every service-layer behavior unchanged.
- All MCP tools and CLI commands backed by services in this package continue to work identically.

## Depends on

P2 complete.

## Parallelizable with

P4, P5, P6, P7.

## Blocks

P8.
