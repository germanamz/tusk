# [v0.14 P6] Sweep filter/ + domain/ + syntax/

> Type: epic (phase parent)
> Milestone: v0.14
> Plan: docs/superpowers/plans/v014-naming-convention/06-sweep-filter-domain.md
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §3

## What

Bring every file in `filter/`, `domain/`, and `syntax/` (production
and tests) into compliance with STYLE.md's four rules. Remove the
six exclusion entries (three packages × two linter configs) from
both linters. Mechanical sweep; no behavior changes. Filter
expressions, urgency scoring, taxonomy validation, and `syntax/`
lex/AST behavior remain identical.

## Why

`filter/`, `domain/`, and `syntax/` together host the parse path
and the core types. Bundling avoids inflating phase count for three
small/medium packages; `syntax/` pairs naturally with `filter/` (it
hosts token, AST, and modifier infrastructure used by parsing).

## Children

- P6-T1: Drop filter/ + domain/ + syntax/ varnamelen exclusions
- P6-T2: Drop filter/ + domain/ + syntax/ pathfilter entries
- P6-T3: Apply STYLE.md fixes across filter/ + domain/ + syntax/
- P6-T4: Verify filter/ + domain/ + syntax/ tests pass
- P6-T5: Verify P6 packages lint clean

## Acceptance

- `make lint` passes against all three packages with no exclusions.
- `make test` passes — filter parser, urgency scoring, taxonomy validation, workflow transitions, and syntax lex/AST behavior unchanged.

## Depends on

P2 complete.

## Parallelizable with

P3, P4, P5, P7.

## Blocks

P8.
