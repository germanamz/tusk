# [v0.14 P2-T4] Add pathfilter helper

> Type: task
> Parent: P2 — Custom analyzers (rules 2, 3, 4)
> Plan: docs/superpowers/plans/v014-naming-convention/02-custom-analyzers.md (task 4)
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §2

## What

Create `internal/lint/pathfilter/pathfilter.go` exporting
`func Excluded(pkgPath string) bool`. The helper consults a
compiled-in slice of **twelve** per-package regexes that mirror the
per-package exclusion rules in `.golangci.yml`. Each analyzer's
`Run` function consults this helper and short-circuits with no
diagnostics when the package is excluded.

Twelve packages: `service`, `internal/tui`, `internal/mcp`,
`internal/portability`, `filter`, `domain`, `syntax`, `repository`,
`sqlite`, `cmd`, `tests/e2e`, plus the module root (`client.go`).

## Why

Per-package entries are intentional: parallel sweep phases each
remove a different entry, so branches do not merge-conflict on a
single shared alternation. Verify the module path from `go.mod`
(`github.com/germanamz/tusk`) before hardcoding into the regex.

## Code references

- `go.mod:1` — module path `github.com/germanamz/tusk`.
- `.golangci.yml` (after P1-T5) — twelve per-package `varnamelen` exclusion rules; pathfilter mirrors the same packages.
- Module-root pattern: `^github\.com/germanamz/tusk$` (matches `client.go`'s root package).

## Acceptance

- `internal/lint/pathfilter/pathfilter.go` exists and exports `Excluded(pkgPath string) bool`.
- The `excluded` slice contains exactly twelve regex entries, each anchored with `(/|$)` for sub-package boundaries (except the module-root entry, which is `^github\.com/germanamz/tusk$`).
- Each entry has a comment noting its removal target phase.
- `Excluded` returns `true` for any package path that matches an entry; `false` otherwise.

## Bridge code

Introduces: twelve per-package exclusion entries in the `excluded` slice.
Removed by: P3-T2, P4-T2, P5-T2, P6-T2, P7-T2; any residual entries removed by P8-T2.

## Depends on

P1 complete.

## Blocks

P2-T1, P2-T2, P2-T3 — analyzers should call `pathfilter.Excluded(pass.Pkg.Path())` from their `Run` functions; that wiring needs the helper to exist (or can be added in the same PR).
