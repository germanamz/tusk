# [v0.14 P1-T3] Create cmd/tusk-lint multichecker shell

> Type: task
> Parent: P1 — Convention doc + linter scaffold + rule 1
> Plan: docs/superpowers/plans/v014-naming-convention/01-convention-and-scaffold.md (task 3)
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §2

## What

Create `cmd/tusk-lint/main.go` that calls
`golang.org/x/tools/go/analysis/multichecker.Main()` with an empty
analyzer list. The binary must compile and exit zero on any input.
Add `golang.org/x/tools` to `go.mod` if not already present.

## Why

Provides the binary entry point that Phase 2 populates with
analyzers. Shipping the empty shell now lets `make lint` wire to a
real binary in this phase rather than waiting for Phase 2.

## Code references

- `cmd/` (existing dir) — add new `tusk-lint/` subdirectory alongside `tusk/`.
- `go.mod:1` — module path is `github.com/germanamz/tusk`; add `golang.org/x/tools` dependency.

## Acceptance

- `cmd/tusk-lint/main.go` exists and compiles.
- `go build ./cmd/tusk-lint` produces a working binary that exits zero with no input.
- A code comment tags the empty analyzer list as bridge code with the removal reference (Phase 2 task 5).
- `golang.org/x/tools` is listed in `go.mod`.

## Bridge code

Introduces: empty analyzer registry in `cmd/tusk-lint/main.go`.
Removed by: P2-T5.

## Depends on

None within phase. May proceed in parallel with P1-T1, P1-T2, P1-T4, P1-T5.

## Blocks

P1-T4 (Makefile target invokes this binary).
P1-T6 (CI verification depends on the binary existing).
