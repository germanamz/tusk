# [v0.14 P6-T2] Drop filter/ + domain/ + syntax/ pathfilter entries

> Type: task
> Parent: P6 — Sweep filter/ + domain/ + syntax/
> Plan: docs/superpowers/plans/v014-naming-convention/06-sweep-filter-domain.md (task 2)

## What

Delete the three regex lines for
`^github\.com/germanamz/tusk/filter(/|$)`,
`^github\.com/germanamz/tusk/domain(/|$)`, and
`^github\.com/germanamz/tusk/syntax(/|$)` from the `excluded`
slice in `internal/lint/pathfilter/pathfilter.go`.

## Why

Brings rules 2, 3, 4 into scope for all three packages.

## Code references

- `internal/lint/pathfilter/pathfilter.go` — locate entries matching `filter`, `domain`, `syntax`.

## Acceptance

- None of the three entries exists in the `excluded` slice after the change.
- All other entries remain.

## Bridge code

Removes: three regex entries in `pathfilter.go` (introduced by P2-T4).

## Depends on

P2 complete.

## Blocks

P6-T3.
