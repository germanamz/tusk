# [v0.14 P3-T5] Verify service/ lint clean

> Type: task
> Parent: P3 — Sweep service/
> Plan: docs/superpowers/plans/v014-naming-convention/03-sweep-service.md (task 5)

## What

Run `make lint` to confirm zero violations across `service/` with
all four rules now active (no exclusions). Closes the phase.

## Why

The phase is "done" when both linters report zero violations
against `service/` and all other tests pass. This is the
acceptance gate.

## Code references

- `service/` — all production and test files.

## Acceptance

- `make lint` exits zero.
- Both `lint-go` (varnamelen across `service/`) and `lint-tusk` (blankline + namederr + testhandle across `service/`) report no diagnostics.
- All MCP tools and CLI commands continue to work identically (spot-check or rely on `make test` from P3-T4).

## Bridge code

None.

## Depends on

P3-T4.

## Blocks

P8 (lock-in cannot start until every sweep phase reaches its lint-clean gate).
