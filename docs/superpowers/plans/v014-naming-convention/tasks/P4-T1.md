# [v0.14 P4-T1] Drop internal/tui/ varnamelen exclusion

> Type: task
> Parent: P4 — Sweep internal/tui/
> Plan: docs/superpowers/plans/v014-naming-convention/04-sweep-tui.md (task 1)

## What

Delete the `linters: [varnamelen]` rule whose `path` is
`^internal/tui/` from `.golangci.yml`. Other per-package rules stay
untouched.

## Why

Brings rule 1 into scope for `internal/tui/`.

## Code references

- `.golangci.yml` `linters.exclusions.rules` — locate the entry with `path: ^internal/tui/`.

## Acceptance

- `.golangci.yml` no longer contains a `linters: [varnamelen]` rule with `path: ^internal/tui/`.
- All other rules remain.

## Bridge code

Removes: `internal/tui/` `varnamelen` exclusion rule (introduced by P1-T5).

## Depends on

P2 complete.

## Blocks

P4-T3.
