# [v0.14 P3-T2] Drop service/ pathfilter entry

> Type: task
> Parent: P3 — Sweep service/
> Plan: docs/superpowers/plans/v014-naming-convention/03-sweep-service.md (task 2)

## What

Delete the regex line for
`^github\.com/germanamz/tusk/service(/|$)` from the `excluded`
slice in `internal/lint/pathfilter/pathfilter.go`. Other entries
stay.

## Why

Removing this entry brings rules 2, 3, 4 into scope for `service/`.
Pairs with P3-T1 (which handles rule 1).

## Code references

- `internal/lint/pathfilter/pathfilter.go` — locate the entry matching `service`.

## Acceptance

- `internal/lint/pathfilter/pathfilter.go`'s `excluded` slice no longer contains the `service` entry.
- All other entries remain.

## Bridge code

Removes: `service/` regex entry in `pathfilter.go` (introduced by P2-T4).

## Depends on

P2 complete.

## Blocks

P3-T3.
