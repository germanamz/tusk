# [v0.14 P1-T1] Write STYLE.md

> Type: task
> Parent: P1 — Convention doc + linter scaffold + rule 1
> Plan: docs/superpowers/plans/v014-naming-convention/01-convention-and-scaffold.md (task 1)
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §1

## What

Author `STYLE.md` at the repo root. Three sections: the four
mechanical rules (with before/after examples and one-line
rationales), the advisory style guide (receivers, generic type
parameters, loop indices), and an enforcement summary mapping each
rule to the linter that enforces it.

## Why

`STYLE.md` is the canonical reference cited from PR templates,
review checklists, and every sweep phase. Without it, sweeps cannot
appeal to a single source of truth.

## Code references

- `docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md:42-191` — full convention text and examples.
- `service/task.go:64` — the `defaultProjectID` block cited as the rule-1 / rule-2 motivating example.
- `service/task.go:325-339` — the `listInBundle` block cited as the rule-3 motivating example.

## Acceptance

- `STYLE.md` exists at the repo root.
- All four rules are documented with at least one before/after example each.
- The style-guide section covers receivers, generic type parameters, and loop indices.
- The enforcement summary names the linter for each rule (`varnamelen` → 1; `tusk-lint -blankline` → 2; `tusk-lint -namederr` → 3; `tusk-lint -testhandle` → 4).
- Rule 3 explicitly states that on shadow, **every** `err` instance is renamed (including the first).

## Bridge code

None.

## Depends on

None within phase. May be authored in parallel with P1-T3, P1-T4, P1-T5.

## Blocks

P1-T2 (cross-reference target must exist).
