# [v0.14 P7-T5] Verify P7 packages lint clean

> Type: task
> Parent: P7 — Sweep repository/ + sqlite/ + cmd/ + tests/e2e/ + root
> Plan: docs/superpowers/plans/v014-naming-convention/07-sweep-rest.md (task 5)

## What

Run `make lint` to confirm zero violations across `repository/`,
`sqlite/`, `cmd/`, `tests/e2e/`, and the root package. Closes the
phase.

## Why

Acceptance gate. After this task ships and assuming P3..P6 have
also shipped, every package in the repo is convention-clean and
Phase 8 can begin.

## Code references

- `repository/`, `sqlite/`, `cmd/`, `tests/e2e/`, `client.go`, `client_test.go`.

## Acceptance

- `make lint` exits zero.

## Bridge code

None.

## Depends on

P7-T4.

## Blocks

P8.
