# [v0.14 P3-T1] Drop service/ varnamelen exclusion

> Type: task
> Parent: P3 — Sweep service/
> Plan: docs/superpowers/plans/v014-naming-convention/03-sweep-service.md (task 1)

## What

Delete the `linters: [varnamelen]` exclusion rule whose `path` is
`^service/` from `.golangci.yml`. Other per-package rules stay
untouched.

## Why

Removing the exclusion is the trigger that brings rule 1 into scope
for `service/`. Subsequent tasks fix violations and verify.

## Code references

- `.golangci.yml` `linters.exclusions.rules` — locate the entry with `path: ^service/`.

## Acceptance

- `.golangci.yml` no longer contains a `linters: [varnamelen]` rule with `path: ^service/`.
- All other `linters: [varnamelen]` exclusion rules and the existing `errcheck` rules remain.

## Bridge code

Removes: `service/` `varnamelen` exclusion rule (introduced by P1-T5).

## Depends on

P2 complete.

## Blocks

P3-T3 (fixes can't be enumerated until the rule is in scope).
