# [v0.14 P4] Sweep internal/tui/

> Type: epic (phase parent)
> Milestone: v0.14
> Plan: docs/superpowers/plans/v014-naming-convention/04-sweep-tui.md
> Spec: docs/superpowers/specs/2026-04-28-v0.14-naming-convention-design.md §3

## What

Bring every file in `internal/tui/` (production and tests) into
compliance with STYLE.md's four rules. Remove the `internal/tui/`
exclusion entries from both linters. Mechanical sweep; no behavior
changes. CLI command output remains byte-identical to pre-sweep
(verified by existing e2e snapshot tests).

## Why

`internal/tui/` is the second-largest package and the CLI surface.
Sweeping it exercises the convention against cobra command builders
and renderer code.

## Children

- P4-T1: Drop internal/tui/ varnamelen exclusion
- P4-T2: Drop internal/tui/ pathfilter entry
- P4-T3: Apply STYLE.md fixes across internal/tui/
- P4-T4: Verify internal/tui/ tests pass
- P4-T5: Verify internal/tui/ lint clean

## Acceptance

- `make lint` passes against `internal/tui/` with no exclusions.
- `make test` passes — every CLI command produces byte-identical output.
- Cobra help text remains unchanged.

## Depends on

P2 complete.

## Parallelizable with

P3, P5, P6, P7.

## Blocks

P8.
