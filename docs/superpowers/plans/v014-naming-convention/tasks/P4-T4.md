# [v0.14 P4-T4] Verify internal/tui/ tests pass

> Type: task
> Parent: P4 — Sweep internal/tui/
> Plan: docs/superpowers/plans/v014-naming-convention/04-sweep-tui.md (task 4)

## What

Run `make test` to verify behavior is preserved after the sweep.
All `internal/tui/` tests, including the e2e tests that exercise
CLI command output, must pass. CLI output remains byte-identical to
pre-sweep.

## Why

CLI output is user-facing and verified by snapshot tests; any
accidental change would surface here.

## Code references

- `internal/tui/*_test.go` and `tests/e2e/` — exercise CLI surface.

## Acceptance

- `make test` exits zero.
- `tusk task create`, `tusk task list`, `tusk task get`, `tusk task modify`, `tusk task tree`, `tusk task next`, etc. produce byte-identical output to pre-sweep behavior.

## Bridge code

None.

## Depends on

P4-T3.

## Blocks

P4-T5.
