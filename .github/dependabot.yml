version: 2

# Weekly dependency updates across the three ecosystems Tusk ships. Each is
# grouped into a single PR to keep CI runs and review noise down. The
# commit-message prefix MUST be a conventional-commit type that both the
# pr-title check (amannn) and commitlint (config-conventional) accept —
# `deps` is NOT in either type-enum, so Go/npm bumps use `build` and
# action bumps use `ci`.
updates:
  - package-ecosystem: gomod
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      go-deps:
        patterns: ["*"]
    commit-message:
      prefix: build

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
    commit-message:
      prefix: ci

  # The unified frontend's deps. The dependabot-frontend-dist rebuild workflow
  # rebuilds the committed dist (internal/webapp/dist) and pushes the result
  # back, so web-deps bot PRs self-heal the dist-drift check without a hand
  # rebuild (as long as DIST_DEPLOY_KEY is registered).
  - package-ecosystem: npm
    directory: "/web"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      web-deps:
        patterns: ["*"]
    commit-message:
      prefix: build
