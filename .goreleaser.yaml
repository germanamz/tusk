version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/tusk
    binary: tusk
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/germanamz/tusk/internal/version.Current=v{{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
  groups:
    - title: Features
      regexp: '^feat'
    - title: Bug Fixes
      regexp: '^fix'
    - title: Others
      order: 999

# The GitHub release is owned by release-please; this workflow runs goreleaser
# with --skip=publish and uploads dist/* to that release. Disabled here so a
# manual `goreleaser release` never creates a competing release.
release:
  disable: true
